# Copy to .env. Do not commit .env.
OPENAI_API_KEY=

# Defects4J / SWE-bench paths (set if not using dataset JSON paths)
DEFECTS4J_HOME=
PERL5_DIR=

# Work root (default: /tmp/trace_work)
TRACE_WORK_ROOT=